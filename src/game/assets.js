// src/game/assets.js
// Placeholder asset loader – in the future this will load images, audio, etc.

export async function loadAssets() {
  // Currently no external assets; return a resolved promise.
  return Promise.resolve();
}
